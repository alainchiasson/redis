---
# User Vars
redis_data_dir: /var/lib/redis
redis_log_dir: /var/log/redis
redis_run_dir: /var/run/redis
redis_conf_dir: /etc

# Note: default for node deployments.
redis_port: 6379

# Role vars - private to role
redis_packages:
  - redis
# defaults file for redis
redis_state: "present"
